namespace Snapshot.ArchiveExploration;

// Bounded context: ArchiveExploration.
//
// Owns the archive viewers (Notion HTML, Gmail mbox) and the diff-tree. v1 spike
// ships a stub view only (see ArchivesPage). Real implementation lands behind
// archive-exploration-001-archive-viewers.
//
// This file is a marker so the BC folder isn't empty; concrete services land here later.
internal static class _Marker { }
